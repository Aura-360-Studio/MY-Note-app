export type Project = {
  id: string;
  name: string;
  createdAt: string;
  updatedAt: string;
};

export const DEFAULT_PROJECT_ID = "general-project";
export const DEFAULT_PROJECT_NAME = "General";
